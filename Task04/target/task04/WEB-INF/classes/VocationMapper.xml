<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ptteng.mapper.VocationMapper">
    <select id="getVocationsWithDirection" parameterType="int" resultType="com.ptteng.model.Vocation">
        SELECT * FROM vocation WHERE v_direction = #{v_direction}
    </select>
</mapper>
